{
  "name": "dechroma",
  "version": "0.1.0",
  "description": "Chroma keyer for canvas video",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "pnpm clean && pnpm declaration && pnpm bundle",
    "clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "ts-node --esm ./esbuild.config.ts",
    "declaration": "tsc --project tsconfig.json",
    "lint": "eslint src --ext .ts",
    "prettier": "npx prettier --write ./src/**/*.{ts,js,json,css,scss,md}"
  },
  "keywords": [],
  "author": "phuritd",
  "license": "MIT",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.60.1",
    "@typescript-eslint/parser": "^5.60.1",
    "esbuild": "^0.18.10",
    "eslint": "^8.43.0",
    "prettier": "^2.8.8",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "files": [
    "dist/**/*"
  ]
}
